name: Scheduled Telegram Briefings

on:
  schedule:
    # 07:28 KST (22:28 UTC previous day) - Pre-warm trigger for 07:30 Morning Briefings
    - cron: '28 22 * * *'
    # 07:31 KST (22:31 UTC previous day) - Fallback trigger for 07:30 Morning Briefings
    - cron: '31 22 * * *'
    # 16:58 KST (07:58 UTC Mon-Sat) - Pre-warm trigger for 17:00 Daily Closing Briefing
    - cron: '58 7 * * 1-6'
    # 17:00 KST (08:00 UTC Mon-Sat) - Exact trigger for 17:00 Daily Closing Briefing
    - cron: '0 8 * * 1-6'
    # 17:02 KST (08:02 UTC Mon-Sat) - Fallback trigger for 17:00 Daily Closing Briefing
    - cron: '2 8 * * 1-6'
  workflow_dispatch:
    inputs:
      briefing_type:
        description: 'Select briefing to execute (all / morning / closing)'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - morning
          - closing
      force:
        description: 'Force send even if already sent today'
        required: false
        default: false
        type: boolean

concurrency:
  group: scheduled-telegram-briefings
  cancel-in-progress: false

jobs:
  send-briefing:
    name: Run Telegram Daily Briefings (07:30 / 17:00)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Execute Telegram Briefings
        run: |
          FLAGS=""
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            FLAGS="$FLAGS --force"
          fi
          if [ "${{ github.event.inputs.briefing_type }}" = "morning" ]; then
            FLAGS="$FLAGS --morning"
          elif [ "${{ github.event.inputs.briefing_type }}" = "closing" ]; then
            FLAGS="$FLAGS --closing"
          fi
          echo "Executing Telegram Briefings with flags: $FLAGS..."
          node scripts/sendTelegramBriefing.mjs $FLAGS
