import React from "react";

// Silent No-Op VersionUpdateBanner (Zero-Reload Mode)
export const syncAppVersionToFirestore = async () => {};

export const VersionUpdateBanner = () => {
  return null;
};

